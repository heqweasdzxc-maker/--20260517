# 变更回执 v2

- 日期：2026-07-18
- 范围：事件态势持久归集、完整首次/最近日期、研判信息三列布局
- 方式：增量部署，预构建前端资源，无服务器联网构建
- 数据：新增 `rw_event_group` 与 `rw_event_group_alarm`，不删除原始告警
- 回滚：自动失败回滚 + 手工回滚脚本
- AI 运行拓扑：不修改
- 视频与摄像机：不修改
- 其他菜单：不修改

## v2 修订原因

- 初版在生产容器启动时发现 `ERR_MODULE_NOT_FOUND: /app/src/integrations/index.mjs`，自动回滚成功，未改变线上功能和数据。
- 根因是初版完整覆盖的后端文件来自包含未部署集成模块的本地源码树，而不是精确生产模块图。
- v2 的 `server.mjs`、`store.mjs` 基于生产当前哈希 `3229cc...`、`87c414...` 重放最小事件归集改动。
- 部署脚本新增容器暂存目录真实 ESM 导入预检；预检成功前不覆盖线上文件。
- ZIP 使用 Unix 权限元数据封装，普通文件为 `0644`、脚本为 `0755`。

## 本地验证

- v2 后端专项：事件生命周期、存储契约和并发告警编号 12/12 通过。
- v2 生产基线 API 回归：46/47 通过；唯一未通过项要求本地测试机存在生产 YOLO26 模型文件，与本增量无关。告警元数据、事件归集、研判闭环及导航 API 均通过。
- v2 模块图：`server.mjs` 完整 ESM 导入通过，确认不再引用生产容器缺失的 `integrations` 模块。
- v2 包体：6/6 包契约检查通过；独立解压后 68 个文件哈希一致，89 个 ZIP 条目权限正确。
- 前端资源未改变初版功能内容；初版已完成 174/174 测试、TypeScript、Vite、OpenAPI 和 Chrome 页面检查。
- 包体不携带地图、训练图片、模型或历史数据副本。
