# River-Watch Field Apply Result 2026-07-07 14:09

Source: pasted server output from `ai-river@airiver-sy` after applying `river-watch-runtime-optimization-20260707.zip`.

## Rule Going Forward

Every material change, field deployment result, fix, verification result, or follow-up diagnosis must be recorded in GitHub under this repository so another agent can continue without relying on the Codex thread.

Repository:

```text
heqweasdzxc-maker/--20260517
```

Primary handoff file:

```text
docs/handoff/2026-07-07-river-watch-runtime-optimization.md
```

This event-specific update file:

```text
docs/handoff/2026-07-07-field-apply-1409.md
```

## Apply Command Used On Server

```bash
cd /home/ai-river
sha256sum river-watch-runtime-optimization-20260707.zip

rm -rf /tmp/river-watch-runtime-optimization-20260707
mkdir -p /tmp/river-watch-runtime-optimization-20260707
unzip -o river-watch-runtime-optimization-20260707.zip -d /tmp/river-watch-runtime-optimization-20260707

APP_DIR=/home/ai-river/river-watch \
bash /tmp/river-watch-runtime-optimization-20260707/scripts/apply-runtime-optimization-20260707.sh
```

## Confirmed Successful

- Package checksum matched:

```text
9d2436e05975e0963206e0bcc4a433b337f7c55148423860ec3c502c49d417dc
```

- Package extracted correctly under:

```text
/tmp/river-watch-runtime-optimization-20260707
```

- Installer ran from `1/8` through `8/8` and ended with `done`.
- Backup directory created:

```text
/home/ai-river/river-watch/backups/runtime-optimization/20260707-140928
```

- Backend hot update succeeded:

```text
deploy-backend-1:/app/src/server.mjs
```

- Frontend hot update succeeded:

```text
deploy-frontend-1:/usr/share/nginx/html/
```

- New AI batch services are active:
  - `river-ai-batch@river.service`
  - `river-ai-batch@structure.service`

- Stream relay services are active:
  - `river-stream-relay@CH01.service` through `river-stream-relay@CH10.service`

- Relay commands use VAAPI hardware path:
  - `-hwaccel vaapi`
  - `-hwaccel_device /dev/dri/renderD128`
  - `-c:v h264_vaapi`
  - `-r 12`
  - output: `rtmp://127.0.0.1:1935/live/CHxx`

## Immediate Performance Observation

The installer's own verification summary initially showed:

- two `batch_worker.py` processes around `8.4%` CPU each,
- ffmpeg relays mostly low CPU after VAAPI,
- backend node process around `2.8%`.

A follow-up command immediately afterward showed a different and important issue:

- `river-ai-batch@river` active,
- `river-ai-batch@structure` active,
- all 10 relay services active,
- ffmpeg relay CPU mostly around `1.9%` to `3.0%`, which confirms the relay transcoding path is improved,
- but the two `batch_worker.py` processes reported extremely high CPU:
  - one around `1448%`,
  - one around `1433%`.

Interpretation:

- Deployment succeeded.
- Frontend/backend hot update succeeded.
- VAAPI relay optimization is working.
- The remaining serious CPU problem has moved to the new batch inference workers.
- This is likely inside batch worker scheduling/inference loop, frame polling, ORT provider behavior, detector configuration, or retry behavior, not ffmpeg software transcoding.

## Security Note

The pasted `ps` output exposed raw RTSP passwords in some ffmpeg command lines. Do not commit raw process output or unmasked RTSP URLs to GitHub. This file intentionally records only masked and summarized values.

## Required Next Investigation Commands

Run on the field server and paste the output back. Mask RTSP credentials before sharing external logs.

```bash
echo "== batch worker logs =="
sudo journalctl -u river-ai-batch@river -u river-ai-batch@structure --since "2026-07-07 14:09:58" --no-pager | tail -200

echo "== batch env =="
sudo grep -RHE '^(BATCH_ID|CHANNELS|MAX_BATCH_SIZE|SCHEDULER_TICK_MS|FRAME_STALE_SEC|HEARTBEAT_SEC|SENTINEL_POLL_SEC|ORT_PROVIDERS|ORT_INTRA_OP_NUM_THREADS|ORT_INTER_OP_NUM_THREADS)=' /etc/river-watch/ai-batch-*.env

echo "== per channel fps/model/provider settings =="
sudo grep -RHE '^(CAMERA_ID|DETECTORS|SAMPLE_FPS|YOLO_ONNX|YOLO_IMGSZ|ORT_PROVIDERS|ORT_INTRA_OP_NUM_THREADS|ORT_INTER_OP_NUM_THREADS)=' /etc/river-watch/ai-worker-CH*.env

echo "== process threads =="
ps -eLo pid,tid,pcpu,comm,args --sort=-pcpu | grep -E 'batch_worker|python' | head -80

echo "== gpu state =="
rocm-smi || true

echo "== service status =="
systemctl status river-ai-batch@river river-ai-batch@structure --no-pager
```

## Priority

High. The system is running, but two batch workers at around 1400% CPU each means the current batch-worker implementation requires immediate tuning or a hotfix before it can be considered production-stable.
